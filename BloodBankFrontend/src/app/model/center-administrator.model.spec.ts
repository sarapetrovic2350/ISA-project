import { CenterAdministrator } from './center-administrator.model';

describe('CenterAdministrator', () => {
  it('should create an instance', () => {
    expect(new CenterAdministrator()).toBeTruthy();
  });
});
