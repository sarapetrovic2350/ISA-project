import { ScheduledAppointment } from './scheduled-appointment.model';

describe('ScheduledAppointment', () => {
  it('should create an instance', () => {
    expect(new ScheduledAppointment()).toBeTruthy();
  });
});
