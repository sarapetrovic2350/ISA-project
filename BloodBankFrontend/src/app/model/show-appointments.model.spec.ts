import { ShowAppointments } from './show-appointments.model';

describe('ShowAppointments', () => {
  it('should create an instance', () => {
    expect(new ShowAppointments()).toBeTruthy();
  });
});
