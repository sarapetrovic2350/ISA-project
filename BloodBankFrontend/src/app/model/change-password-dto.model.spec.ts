import { ChangePasswordDTO } from './change-password-dto.model';

describe('ChangePasswordDTO', () => {
  it('should create an instance', () => {
    expect(new ChangePasswordDTO()).toBeTruthy();
  });
});
