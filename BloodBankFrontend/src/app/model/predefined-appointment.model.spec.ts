import { PredefinedAppointment } from './predefined-appointment.model';

describe('PredefinedAppointment', () => {
  it('should create an instance', () => {
    expect(new PredefinedAppointment()).toBeTruthy();
  });
});
