import { AppointmetForCalendar } from './appointmet-for-calendar';

describe('AppointmetForCalendar', () => {
  it('should create an instance', () => {
    expect(new AppointmetForCalendar()).toBeTruthy();
  });
});
